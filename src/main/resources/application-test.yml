spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/crudresttest
    username: crudrest
    password: crudrest

logging:
  level:
    org:
      hibernate:
        type: trace













